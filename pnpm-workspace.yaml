packages: []

overrides:
  vite: npm:@voidzero-dev/vite-plus-core@0.1.11
  vitest: npm:@voidzero-dev/vite-plus-test@0.1.11

catalog:
  # AWS CDK
  aws-cdk-lib: "2.60.0"
  aws-cdk: "2.60.0"
  constructs: "^10.0.0"

  # esbuild is required for aws-cdk-lib's NodejsFunction construct.
  # NodejsFunction internally uses esbuild to bundle Lambda function code.
  # Without esbuild in devDependencies, CDK falls back to Docker-based bundling,
  # which is slower and requires Docker to be installed and running.
  esbuild: "^0.27.2"

  # Type definitions
  "@types/aws-lambda": "^8.10.115"
  "@types/node": "24.10.9"

  # TypeScript
  typescript: "^5.9.3"

  # Build & Dev tools
  vite-plus: "0.1.11"
